<form method="post" action="{{ path('app_notes_delete', {'id': note.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ note.id) }}">
    <button class="btn btn-primary w-100 mb-2">Удалить</button>
</form>
